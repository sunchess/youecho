<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :title %>
  <%= text_input f, :title %>
  <%= error_tag f, :title %>

  <%= label f, :translate %>
  <%= text_input f, :translate %>
  <%= error_tag f, :translate %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
